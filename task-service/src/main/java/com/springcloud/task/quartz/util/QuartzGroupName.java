package com.springcloud.task.quartz.util;

public interface QuartzGroupName {
    public enum JobGroupName {
        DEFAULT_JOB_GROUP,
    };
    public enum TriggerGroupName {
        DEFAULT_TRIGGER_NAME
    }
}
